id: tufw
name: tufw
description: Terminal UI for ufw
categories:
  - dashboards
binaries:
  - tufw
homepage: https://github.com/peltho/tufw
repo: https://github.com/peltho/tufw
language: Go
install:
  apt: apt install tufw_0.1.0_linux_amd64.deb
packages:
  apt: tufw_0.1.0_linux_amd64.deb
installNotes: |-
  ## Installation
  Just head over the [releases](https://github.com/peltho/tufw/releases) page and install it manually with your favorite package manager.

  For instance: ```sudo apt install tufw_0.1.0_linux_amd64.deb```

  You can also find this package on **AUR** for archlinux users: ```yaourt -S tufw-git```
readmeUrl: https://raw.githubusercontent.com/peltho/tufw/HEAD/README.md
