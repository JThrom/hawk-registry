id: pomo
name: pomo
description: A minimal, customizable TUI Pomodoro timer with ASCII art, progress bar, desktop notifications, and productivity statistics.
categories:
  - productivity
binaries:
  - pomo
homepage: https://github.com/Bahaaio/pomo
repo: https://github.com/Bahaaio/pomo
language: Go
tags:
  - minimal
  - customizable
  - pomodoro
  - timer
  - ascii
  - progress
  - desktop
  - notifications
  - productivity
  - statistics
install:
  brew: brew install --cask bahaaio/pomo/pomo
  go: go install github.com/Bahaaio/pomo@latest
packages:
  brew: pomo
  go: pomo
installNotes: |-
  ## Installation

  ### Homebrew (macOS)

  ```bash
  brew install --cask bahaaio/pomo/pomo
  ```

  ### Winget (Windows)

  ```powershell
  winget install Bahaaio.pomo
  ```

  ### Go

  ```bash
  go install github.com/Bahaaio/pomo@latest
  ```

  ### Pre-built Binaries

  Download pre-built binaries from the [releases page](https://github.com/Bahaaio/pomo/releases/latest).

  ### Nix

  <details>
  <summary>❄️ Nix flake installation</summary>

  **Option 1: Add to flake inputs and system packages**

  In your `flake.nix`:

  ```nix
  pomo = {
    url = "github:Bahaaio/pomo";
    inputs.nixpkgs.follows = "nixpkgs";
  };
  ```

  Then add to your system packages:

  ```nix
  environment.systemPackages = with pkgs; [
    inputs.pomo.packages.${pkgs.stdenv.hostPlatform.system}.default
  ];
  ```

  **Option 2: Run directly**

  ```bash
  nix run github:Bahaaio/pomo
  ```

  </details>

  ### Build from Source

  ```bash
  git clone https://github.com/Bahaaio/pomo
  cd pomo
  go build .
  ```
readmeUrl: https://raw.githubusercontent.com/Bahaaio/pomo/HEAD/README.md
