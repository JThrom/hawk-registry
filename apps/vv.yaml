id: vv
name: vv
description: A terminal image viewer, supporting an extensive range of modern image formats
categories:
  - media
binaries:
  - vv
homepage: https://github.com/wolfpld/vv
repo: https://github.com/wolfpld/vv
language: Go
tags:
  - image
  - supporting
  - extensive
  - range
  - formats
installNotes: |-
  ## Building

  Follow the standard CMake build process. It will create the `build/vv` executable.

  ```
  cmake -B build -DCMAKE_BUILD_TYPE=Release
  cmake --build build
  ```

  By default vv is built with the `-march=native` compiler option, to enable SIMD processing. If you want to build an executable that will work on any machine you may want to turn this off with the `MARCH_NATIVE` CMake option.
readmeUrl: https://raw.githubusercontent.com/wolfpld/vv/HEAD/README.md
